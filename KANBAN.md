# 용돈 기입장 구현 칸반

기준 문서: [requirements.txt](requirements.txt). 필수 카드 K01–K16 및 선택 카드 B01–B03 구현을 완료하고 Python 3.12에서 검증했습니다.

카드는 `할 일 → 진행 중 → 검토/검증 → 완료` 순서로 이동합니다. 카드의 인수 조건을 충족하고 관련 검증을 통과하면 완료로 이동합니다.

## 구현 기준

- Python 3.10 이상, 표준 라이브러리만 사용합니다.
- 실행 진입점은 `python -m budget_app <command> [options]`입니다.
- 옵션은 상세 CLI 요구사항에 따라 `--`로 통일합니다. 문서 일부의 `-top`, `-data-dir`, 단일 `-` 표기는 각각 `--top`, `--data-dir` 등으로 정규화합니다.
- 저장 형식은 JSONL로 선택하고, 기본 `./data` 아래 `transactions.jsonl`, `categories.jsonl`, `budgets.jsonl`을 사용합니다. CSV는 가져오기/내보내기용입니다.
- `add`는 대화형, `update`는 옵션 방식으로 고정합니다. 초기 카테고리는 `food`, `transport`, `rent`, `salary`, `etc`를 자동 생성합니다.
- 최신순은 거래 날짜 내림차순으로 정의하고, 같은 날짜는 저장 순서의 역순으로 정렬합니다. 조회/검색은 전체 거래를 메모리에 적재하지 않는 방식으로 구현합니다.
- 아래 세부 정책은 구현 계획의 선택입니다: 사용 중인 카테고리 삭제 차단, 잘못된 import 행은 건너뛰고 원인/건수 보고, 월과 기간 export 조건의 동시 지정은 오류 처리.

## 할 일

없음.

## 진행 중

없음.

## 검토/검증

없음.

## 완료 — 필수

| ID | 작업 | 인수 조건 | 선행 카드 |
| --- | --- | --- | --- |
| K01 | 프로젝트 구조 및 CLI 진입점 | `budget_app` 패키지를 만들고 모델/저장소/서비스/CLI 등 최소 3개 모듈로 책임을 분리한다. 최소 2개 클래스를 사용한다. 모든 명령과 하위 명령에서 `--help`를 제공한다. | 없음 |
| K02 | 거래 모델 및 공통 입력 검증 | `Transaction` dataclass에 유일한 `id`, `type`, `date`, 양수 정수 `amount`, `category`, 선택 `memo`/`tags`를 정의한다. 실제 유효한 날짜·월, 허용된 타입, 등록된 카테고리를 검사한다. 함수 입출력과 데이터 구조에 타입 힌트를 적용한다. | K01 |
| K03 | 영구 저장 및 초기화 | JSONL 파일 3개를 생성하고 재실행 후 거래/카테고리/예산을 유지한다. `--data-dir`로 경로를 바꿀 수 있다. 빈 카테고리 파일에 기본 카테고리를 생성한다. 거래 읽기는 `yield` 기반 제너레이터로 제공한다. | K02 |
| K04 | 공통 오류 처리 데코레이터 | 예외 처리 데코레이터를 최소 1개 구현하고 실제 명령 실행에 적용한다. 입력 오류, 손상된 저장 데이터, 파일 접근 실패에 원인과 해결 힌트를 출력한다. 스택트레이스 없이 정상 종료 0 / 오류 종료 비0을 반환한다. | K01 |
| K05 | 카테고리 관리 | `category add/list/remove`를 구현한다. 추가/삭제 대상은 대화형으로 입력받는다. 목록과 처리 결과를 출력하며 중복/없는 카테고리는 안내한다. 거래에서 사용 중인 카테고리는 삭제를 차단한다. | K03, K04 |
| K06 | 거래 추가 | `add`에서 날짜/타입/카테고리/금액/메모/태그를 순차 입력받는다. 잘못된 입력은 재입력 또는 원인/힌트로 처리한다. 저장 후 생성된 유일 ID를 출력한다. | K02, K03, K04, K05 |
| K07 | 최신순 스트리밍 조회 기반 | 파일 읽기부터 조회까지 제너레이터 기반 흐름을 유지한다. 역순 읽기만으로 날짜순을 가정하지 않는다. 제한 조회는 제한된 메모리를, 전체 정렬 조회는 외부 정렬 등 디스크 기반 방식을 사용한다. 날짜가 뒤섞인 입력에서도 정해진 최신순을 보장한다. | K03 |
| K08 | 거래 목록 | `list --limit N`을 구현한다. 기본 제한값을 문서화하고 양수 여부를 검증한다. 최신순 결과와 빈 목록 안내를 출력한다. | K04, K07 |
| K09 | 거래 검색 | `search --from --to --category --type --q --tag`를 지원한다. 기간은 양 끝 날짜를 포함하고, 여러 조건은 AND로 결합한다. 메모 키워드와 태그를 검색하며, 잘못된 기간을 안내한다. 결과는 스트리밍 기반 최신순으로 출력한다. | K02, K04, K07 |
| K10 | 안전한 거래 수정/삭제 | `update --id`와 선택 `--date/--type/--category/--amount/--memo/--tags`, `delete --id`를 구현한다. 수정 데이터도 검증하고 ID는 유지한다. 없는 ID는 오류로 안내한다. 임시 파일 작성 후 원자적 교체로 반영하며 실패 시 기존 파일을 보존한다. | K02, K03, K04 |
| K11 | 월별 요약 | `summary --month YYYY-MM --top N`으로 총수입/총지출/잔액과 카테고리별 지출 TOP N을 출력한다. 제너레이터로 합산하고, 잘못된 월/순위 개수를 검증한다. 거래가 없는 월은 데이터 없음을 명시한다. | K02, K03, K04 |
| K12 | 월 예산 설정 및 조회 | `budget set --month YYYY-MM --amount 금액`으로 월 예산을 저장/갱신한다. 양수 정수 금액을 검증한다. `summary`에서 설정된 예산, 지출 대비 사용률, 초과 시 경고를 표시한다. 재실행 후에도 유지한다. | K03, K04, K11 |
| K13 | CSV 가져오기 | `import --from <csv>`를 구현한다. UTF-8 헤더와 필수 열을 확인하고 행별 날짜/타입/카테고리/금액을 검증한다. 정상 행에 새 ID를 부여해 저장한다. 잘못된 행은 행 번호·원인·힌트와 함께 건너뛰며 imported/skipped 건수를 출력한다. 일부 행 실패 시에도 비0 종료한다. | K02, K03, K04, K05 |
| K14 | CSV 내보내기 | `export --out <csv>`에 `--month` 또는 `--from/--to` 기간 조건을 필수로 받는다. 기간 방식은 한쪽 경계만 지정해도 허용한다. 월/기간 혼용과 역전 기간은 오류로 안내한다. 조건에 맞는 CSV와 처리 건수를 출력하고, 결과가 없어도 헤더를 생성한다. | K03, K04, K09 |
| K15 | 통합 및 실패 상황 검증 | 표준 라이브러리 `unittest`와 임시 디렉터리로 CRUD, 복합 검색, 최신순/limit, 요약/TOP N, 예산 초과, 카테고리 삭제 차단, CSV 왕복을 검증한다. 재실행 영속성, 모든 help, 종료 코드, 잘못된 입력/없는 ID/손상 파일, 교체 실패 시 원본 보존, 대량 조회의 메모리 사용도 확인한다. | K05–K14 |
| K16 | 실행 및 설계 문서 | README에 Python 버전, 실행법, 저장 파일 경로/형식, `--data-dir`, 초기화 정책, 모든 주요 명령 예시, 옵션 기반 update, CSV 스키마/예시, import 실패 정책, 최신순 기준을 설명한다. 계층별 책임과 제너레이터/데코레이터/타입 힌트의 역할 및 검증 실행법을 기록한다. | K01–K15 |

### CSV 공통 계약 — K13/K14

UTF-8, 헤더 포함. 열 순서는 `date,type,category,amount,memo,tags`로 문서화합니다. import는 필수 열을 요구하고 선택 열 누락을 허용합니다.

| 열 | 필수 | 규칙 |
| --- | --- | --- |
| date | 예 | 유효한 `YYYY-MM-DD` |
| type | 예 | `income` 또는 `expense` |
| category | 예 | 등록된 카테고리 |
| amount | 예 | 양수 정수 |
| memo | 아니요 | 문자열 |
| tags | 아니요 | 쉼표 구분 문자열. CSV 인용/이스케이프는 `csv` 모듈로 처리 |

## 완료 — 선택 백로그

선택 기능도 구현 및 검증을 완료했습니다.

| ID | 작업 | 인수 조건 | 선행 카드 |
| --- | --- | --- | --- |
| B01 | 백업 | `backup`으로 타임스탬프가 포함된 백업을 만들고 거래/카테고리/예산 데이터를 보존한다. 복구 방법을 문서화한다. | K03, K04 |
| B02 | 반복 내역 | 월급/월세 등의 규칙을 영구 저장하고 지정 월의 거래를 생성한다. 같은 규칙/월 재실행 시 중복 생성을 방지한다. | K06, K15 |
| B03 | 콘솔 표 정렬 | 외부 라이브러리 없이 목록/검색/요약을 정렬해 출력한다. 한글과 긴 메모의 표시를 확인한다. | K08, K09, K11 |

요구사항의 선택 항목인 저장 원자성 강화는 데이터 안전성을 위해 필수 구현 계획 K10에 포함했습니다.


## 완료 근거

- K01–K04: `budget_app` 패키지의 모델/저장소/서비스/CLI 분리, 타입 힌트, 실제 오류 처리 데코레이터, 자동 초기화 및 JSONL 영속성 구현.
- K05–K14: 모든 필수 명령 구현. `tests/test_budget_app.py`에서 CRUD/검색/요약/예산/카테고리/CSV와 실패 상황 검증.
- K07/K15: 12,000건 최신순 정렬을 검증하고 `tracemalloc` 기준 Python 최고 메모리 사용량 4 MiB 미만 확인. SQLite 네이티브 메모리는 이 측정에 포함하지 않음.
- K10/K15: `os.replace` 실패를 주입하여 기존 데이터와 임시 파일 정리 확인. 모든 명령 및 하위 명령의 help와 오류 종료 코드 검증.
- K16: README에 실행 환경, 명령 예시, 파일 위치/형식, CSV 계약, 구현 정책, 구조 및 테스트 방법 기록.
- B01–B03: ZIP 백업 내용, 반복 생성/재실행 중복 방지/말일 조정, 한글과 긴 텍스트 표시 검증.
- 검증 명령: `python3.12 -m unittest discover -s tests -v`.
